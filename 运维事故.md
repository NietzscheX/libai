```
 1.上次在git服务器上安装gitlab,使用的rpm安装,结果导致重新新建了git账户,更改了git账户的家目录,导致线上git仓库无法登陆/无法推送代码,
   即使卸载了gitlab的rpm包依然有问题.
   正确解决办法是:使用docker版本的gitlab减少干扰.
 
 2.2016.09.28,收到报警docker仓库服务器磁盘满了,而且难以清理数据,故新加一块硬盘,然后忘了停服务,直接使用mv导致出错,而且无法恢复.
   docker自己在/docker/lib/docker/devicemapper/devicemapper下管理了100GB的data文件和2GB的metadata文件.这个文件难以移动/
   如果不移动docker也会自动生成,所以对于docker本身来说,我还没搞清楚,他的文件组织结构.
   正确的解决办法是:先用rsync将本地文件拷贝到目的磁盘,再停掉业务后同步,直至两边数据一致.
     

经过2次故障,总结教训:运维工作需要胆大心细,不出事则以,一出就是大事,一定要做好整体规划后再实施,
                  要考虑到人为操作风险,务必备份以最低代价恢复.
   
```
